app/FusionCourt/.build/
app/FusionCourt/default.metallib

# Build outputs of the market-shear reproduce scripts. The SOURCES are the artefact;
# these are what a reader's own run produces beside them, and a clean clone that goes
# dirty the moment someone reproduces a figure teaches people to ignore `git status`.
reproduce/extraction-exact
reproduce/extraction-core-syms.txt
reproduce/wasi-sandwiched
reproduce/.core-detector.swift
reproduce/.core-pin.swift
reproduce/logs/
